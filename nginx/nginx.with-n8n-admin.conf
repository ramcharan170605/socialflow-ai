# Optional: expose n8n editor at /n8n/ (MVP admin only — use basic auth in production)
# Usage: docker compose --profile n8n-admin up

worker_processes auto;
events { worker_connections 1024; }

http {
    upstream socialflow_app { server app:3000; }
    upstream n8n_backend { server n8n:5678; }

    server {
        listen 80;
        client_max_body_size 4m;

        location /n8n/ {
            proxy_pass http://n8n_backend/;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_read_timeout 300s;
        }

        location /api/ {
            proxy_pass http://socialflow_app;
            proxy_read_timeout 180s;
            proxy_set_header Host $host;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        location / {
            proxy_pass http://socialflow_app;
            proxy_set_header Host $host;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }
}
